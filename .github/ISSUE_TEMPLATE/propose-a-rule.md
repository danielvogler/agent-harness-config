---
name: Propose a rule, skill or command
about: Something the shared config should say or do
title: 'propose: '
labels: proposal
---

## What should it say or do?

## Which layer, and why?

<!-- From AGENTS.md:
     skill    — works in every harness; the default when unsure
     command  — a repeatable prompt you invoke as /name; everything but Codex
     rule     — passively shapes behaviour; Claude, Cursor, Antigravity only
     hook     — the only layer that can actually BLOCK something -->

- [ ] skill
- [ ] command
- [ ] rule
- [ ] hook

## What went wrong that made you want this?

<!-- A rule with a real incident behind it survives review. One that sounds sensible in
     the abstract usually turns out to be someone's preference. -->

## If it is a rule: can it be `paths:`-scoped?

<!-- An unscoped rule is loaded in every session by every person, in every repo.
     `make context` shows what the current ones cost. -->
