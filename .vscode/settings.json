{
  "// why this file is committed": [
    "`.ecc/` is 240 MB / 11,255 files, including a 140 MB node_modules with 5,486 js/ts/json",
    "files, and it is a git clone in its own right. Left alone, an editor indexes all of it,",
    "watches it, searches it, and shows it as a second source-control provider with hundreds",
    "of changes. None of that is useful: .ecc/ is generated by `make install` and must never",
    "be edited.",
    "",
    "files.exclude is the setting that actually saves memory — it stops the JS/TS language",
    "service walking the tree, not just the search index. The trade-off is that .ecc/ is",
    "hidden from the explorer; read it from a terminal, or see notes/example-hooks/ for the",
    "hook sources worth reading.",
    "",
    "autoRepositoryDetection false is what suppresses the second git provider;",
    "ignoredRepositories alone did not. Requires Cmd+Shift+P -> Developer: Reload Window.",
    "",
    "Delete this file if you disagree — nothing depends on it."
  ],

  "files.exclude": {
    "**/.ecc": true,
    "**/research": true
  },

  "files.watcherExclude": {
    "**/.ecc/**": true,
    "**/research/**": true
  },

  "search.exclude": {
    "**/.ecc": true,
    "**/research": true
  },

  "git.autoRepositoryDetection": false,
  "git.ignoredRepositories": [".ecc", "./.ecc"],
  "git.openRepositoryInParentFolders": "never",

  "typescript.tsserver.maxTsServerMemory": 2048,
  "search.followSymlinks": false
}
